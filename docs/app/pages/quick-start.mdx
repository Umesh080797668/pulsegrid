---
title: Quick Start
---

# Quick Start

This page gives a minimal quick start to run PulseGrid services locally for development.

1. Start core services (see repository README).
2. Run the API gateway:

```bash
cd api-gateway
npm install
npm run dev
```

3. Open the interactive API docs at `http://localhost:3000/api-docs`.
